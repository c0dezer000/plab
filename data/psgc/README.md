Drop PSGC JSON files here (regions, provinces, cities, barangays).

Recommended file names:
- regions.json
- provinces.json
- cities.json
- barangays.json

Each file should be a JSON array. Example:
[
  { "code": "01", "name": "Region I (Ilocos Region)" },
  ...
]

If you want, I can also create an import script to load these into the database once you paste them.